{% extends 'base_logged_in.html.twig' %}

{% block title %}{{ 'key_management.list'|trans }}{% endblock %}

{% block logged_in_content %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="bgc-white bd bdrs-3 p-20 mB-20">
                    <h4 class="c-grey-900 mB-20">
                        {{ 'key_management.list'|trans }}

                        <div class="float-end">
                            <a href="{{ path('app_key_management_new') }}" class="btn btn-success">{{ 'create_new'|trans }}</a>
                        </div>
                    </h4>

                    {% for label, messages in app.flashes(['success', 'danger', 'warning', 'info']) %}
                        {% for message in messages %}
                            <div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
                                {{ message|trans }}
                                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                            </div>
                        {% endfor %}
                    {% endfor %}

                    <table class="mcDataTable table table-striped table-bordered" cellspacing="0" width="100%">
                        <thead>
                        <tr>
                            <th>{{ 'key_management.name'|trans }}</th>
                            <th>{{ 'key_management.status'|trans }}</th>
                            <th>{{ 'key_management.borrow_date'|trans }}</th>
                            <th>{{ 'key_management.return_date'|trans }}</th>
                            <th>{{ 'action'|trans }}</th>
                        </tr>
                        </thead>
                        <tfoot>
                        <tr>
                            <th>{{ 'key_management.name'|trans }}</th>
                            <th>{{ 'key_management.status'|trans }}</th>
                            <th>{{ 'key_management.borrow_date'|trans }}</th>
                            <th>{{ 'key_management.return_date'|trans }}</th>
                            <th>{{ 'action'|trans }}</th>
                        </tr>
                        </tfoot>
                        <tbody>
                        {% for key in keys %}
                            <tr>
                                <td><a href="{{ path('app_key_management_edit', { id: key.id }) }}">{{ key.name }}</a></td>
                                <td><a href="{{ path('app_key_management_edit', { id: key.id }) }}">
                                    <span class="badge bg-{{ key.status.value == 'available' ? 'success' : (key.status.value == 'borrowed' ? 'warning' : 'danger') }}">
                                        {{ key.status.getLabel()|trans }}
                                    </span>
                                    </a></td>
                                <td><a href="{{ path('app_key_management_edit', { id: key.id }) }}">{{ key.borrowDate ? key.borrowDate|date('d.m.Y') : '-' }}</a></td>
                                <td><a href="{{ path('app_key_management_edit', { id: key.id }) }}">{{ key.returnDate ? key.returnDate|date('d.m.Y') : '-' }}</a></td>
                                <td class="text-nowrap">
                                    <a href="{{ path('app_key_management_edit', {id: key.id}) }}" title="{{ 'edit'|trans }}">
                                        <span class="badge bg-info"><i class="ti-pencil-alt"></i></span>
                                    </a>
                                    {% if is_granted('ROLE_ADMIN') %}
                                        <a href="{{ path('app_key_management_delete', {id: key.id}) }}" class="confirmRemoveItem" title="{{ 'delete'|trans }}" role="button" data-confirm="{}" data-confirm-message="{{ 'confirm_remove'|trans }}">
                                            <span class="badge bg-danger"><i class="ti-trash"></i></span>
                                        </a>
                                    {% endif %}
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
